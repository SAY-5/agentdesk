services:
  api:
    build:
      context: .
      dockerfile: Dockerfile.api
    ports:
      - "8000:8000"
    environment:
      PORT: "8000"

  web:
    build:
      context: ./web
    ports:
      - "5173:5173"
    depends_on:
      - api
